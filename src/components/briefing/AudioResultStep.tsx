// This component is no longer used - audio mode now follows the standard order flow.
// Keeping as empty export for backwards compatibility.
export const AudioResultStep = () => null;
export default AudioResultStep;
